# Demo a scheduled workflow
name: Scheduled Coverage
on:
 workflow_dispatch: # Hack to kickstart the scheduled trigger
 schedule: 
   - cron: '20 20 * * *' # (20:20 - 5 = 15:20) 
jobs:
  call-build:
    uses: ./.github/workflows/build.yml

  call-test:
    needs: call-build
    uses: ./.github/workflows/test.yml
